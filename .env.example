# ─────────────────────────────────────────────────────
# KanjiDen MCP Server — Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to git
# ─────────────────────────────────────────────────────

# Supabase project URL (safe to share — not a secret)
# Found in: Supabase dashboard → Settings → API → Project URL
SUPABASE_URL=https://nxgvevmhdzjsjbiuvcqv.supabase.co

# Supabase service role key (SECRET — never expose)
# Found in: Supabase dashboard → Settings → API → service_role key
# This bypasses RLS — only lives here, never in code or chat
SUPABASE_SERVICE_KEY=sb_secret_kv7lbj2NAdqVeHy3THXHUg_FeLDuo6W

# Gateway key — what Claude sends to authenticate with this server
# Must match what you set in your Claude Project system prompt
# Choose anything: "kanjiden-bibs-2026" or any strong passphrase
GATEWAY_KEY=kanjiden-bibs-2026
